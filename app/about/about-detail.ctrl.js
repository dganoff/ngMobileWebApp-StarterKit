(function() {
	angular
		.module('mwa')
		.controller('aboutDetailCtrl', aboutDetailCtrl);

	/* @ngInject */
	function aboutDetailCtrl($scope) {
		// Assign all bindable models:
		

		// Kicks off the controller:
		activate();

		//////////

		/**
		 * Kick off the controller with this function
		 */
		function activate() {
		}
	}
})();