(function() {
	angular
		.module('mwa')
		.controller('homeDetailCtrl', homeDetailCtrl);

	/* @ngInject */
	function homeDetailCtrl($scope) {
		// Assign all bindable models:
		

		// Kicks off the controller:
		activate();

		//////////

		/**
		 * Kick off the controller with this function
		 */
		function activate() {
		}
	}
})();